# The club model's training (club_train.py), on the gaming PC; not the server's.
# PyTorch comes first, from PyTorch's own index for the GPU (see HOME-SETUP.md, "Training the club
# model"): the torch on PyPI for Windows is CPU-only.
ultralytics>=8.4.0,<9
albumentations>=2.0,<3
onnx>=1.17
onnxslim>=0.1.50
onnxruntime>=1.20
pyyaml>=6
